# Проект по распознованию жестов для контроля стороннего плеера

## Установка

```console
foo@bar:~$ cd rec/
foo@bar:~$ make clean
foo@bar:~$ qmake rec.pro #(if qmake installed under /opt/Qt/...)
foo@bar:~$ make -j8
```
